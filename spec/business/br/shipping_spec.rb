RSpec.describe Business::Br::Shipping do
  it "has a version number" do
    expect(Business::Br::Shipping::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
